fragment CardSnippet on Card {
    id
    kind
    createdAt
    updatedAt
    
    frontText
    rearText
    dueAt
    engine

    __typename

    tags {
        ...TagSnippet
    }

    creator {
        id
        email
    }
}